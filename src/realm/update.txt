exports = function(payload, response) {
	var user = {}
	var result = {}

	if (payload.body) {
		user = EJSON.parse(payload.body.text());
		console.log("Parsed Payload body: ", JSON.stringify(user));

		var collection = context.services.get("mongodb-atlas").db("Rebajas").collection("Rebajas");

		return collection.updateOne({_id:BSON.ObjectId(user.user_id)}, {$set: user});
	}
	return result;
};